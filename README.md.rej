diff a/README.md b/README.md	(rejected hunks)
@@ -87,7 +87,8 @@ Surge、Quantumult X、Karing 不是 mihomo 内核，也不认 masque，导进
 
 41 个节点是同一个 WARP 账号的不同接入地址，**出口 IP 是一样的**。
 多节点是为了某个地址被墙时能自动换一个，不是多国家落地。
-想选国家得用 WARP+ 或 ZeroTrust，这个仓库不支持。
+想选国家要么套娃落地（Opera/Proton/Windscribe，Worker 版自带），
+要么用 Zero Trust 把骨干换稳（出口仍由任播决定，不能直接选国家）。
 
 里面有 20 个 IPv6 节点，你没 IPv6 的话它们会连不上，但客户端会自动跳过，
 不影响用。
