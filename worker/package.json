{
  "name": "opera-masque-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "node build.mjs",
    "deploy": "wrangler deploy",
    "test": "node test/auth.test.mjs && node test/config.test.mjs && node test/route.test.mjs && node test/ui.test.mjs && node test/dist.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "wrangler": "^4"
  }
}
